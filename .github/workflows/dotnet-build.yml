name: CI Check

on: [push]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Check files exist
      run: |
        echo "✅ Repository contains:"
        ls -la
        echo ""
        echo "✅ C# files found:"
        find . -name "*.cs" -type f
